import React from 'react';
import './Loader.css'; // Import loader-specific CSS

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
